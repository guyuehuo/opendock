For some Linux cluster systems, 
the multi CPU parallelism of the protocol may have certain issues that prevent it from running, 
such as the need to allocate resources beyond the limit. 
Therefore, an alternative protocol is provided to implement multi CPU parallel sampling, including an example script
